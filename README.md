# CNN-Keras
The goal of this project is to correctly identify digits from a dataset of tens of thousands of handwritten images. We achieve this by building and training a Convolutional Neural Network (CNN), a class of deep neural networks most commonly applied to analyzing visual imagery.
